# Health Check Monorepo

Quickstart

1. Install dependencies: `pnpm install`
2. Start local infra: `pnpm run compose:up`
3. Run dev servers: `pnpm run dev:all`

Verify

- Backend: `http://localhost:3000/health`
- Frontend: `http://localhost:5173`
